import { QueryScheduler } from '../batching';

describe('QueryScheduler', () => {
  it('should construct', () => {
    const scheduler = QueryScheduler({
      options: {
        forceFetch: true,
      },

  });
});